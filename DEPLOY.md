# Deploying Metra

The deployment runbook lives in **[docs/DEPLOY.md](docs/DEPLOY.md)** — Cloudflare
Workers, migrations and RLS, secret rotation, and the cron Worker.

This pointer exists because that is the first place anyone looks. It is
deliberately not a second copy: a runbook that exists twice is a runbook that is
wrong in one place.
